% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_model.R
\name{TrainRankModel}
\alias{TrainRankModel}
\title{Train Multinomial Rank-Based Model}
\usage{
TrainRankModel(data, labels, alpha = 0.1, cv = FALSE, nfolds = 5, ...)
}
\arguments{
\item{data}{A gene expression matrix with genes as rows and cells
(or spatial spots) as columns.
Can be a Seurat object, a dense numeric matrix,
or a sparse \code{dgCMatrix}.}

\item{labels}{A vector of cell type labels (one per column of \code{data}).}

\item{alpha}{Elastic net mixing parameter. 1 = LASSO, 0 = Ridge.
Default is \code{0.1}.}

\item{cv}{Logical. If \code{TRUE}, performs cross-validation using
\code{cv.glmnet}. Default is \code{FALSE}.}

\item{nfolds}{Number of folds for cross-validation
(only used if \code{cv = TRUE}). Default is \code{5}.}

\item{...}{Additional arguments passed to \code{\link{ComputeRankedMatrix}}.}
}
\value{
A fitted \code{glmnet} or \code{cv.glmnet} model object.
}
\description{
This function trains a multinomial logistic regression model
using ranked gene expression. Optionally performs cross-validation
to select the optimal regularization parameter.
}
\examples{
mat <- matrix(runif(1000), nrow = 100)
labels <- sample(c("TypeA", "TypeB"), ncol(mat), replace = TRUE)
set.seed(42)
model <- TrainRankModel(mat, labels, cv = TRUE, nfolds = 3)

}
\seealso{
\code{\link{ComputeRankedMatrix}}, \code{\link{PredictRankModel}}
}
