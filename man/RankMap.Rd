% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_model.R
\name{RankMap}
\alias{RankMap}
\title{RankMap: Train and Predict Cell Types Using Top-Ranked Genes}
\usage{
RankMap(
  data = NULL,
  labels = NULL,
  new_data = NULL,
  n_feature_max = 500,
  k = 20,
  alpha = 0.1,
  seed = 42,
  cv = FALSE,
  nfolds = 5,
  lambda = NULL,
  return_probs = FALSE,
  return_confidence = TRUE,
  threshold = NULL,
  return_model = FALSE,
  ...
)
}
\arguments{
\item{data}{Reference gene expression matrix (genes × cells),
or a Seurat object.}

\item{labels}{A character or factor vector of cell type labels for
columns of \code{data}.}

\item{new_data}{New data to annotate. Same format as \code{data}
(matrix, \code{dgCMatrix}, or Seurat object).}

\item{n_feature_max}{Maximum number of genes to use when more than
500 genes are shared. Default is \code{500}.}

\item{k}{Number of top expressed genes to retain per cell (ranking).
Default is \code{20}.}

\item{alpha}{Elastic net mixing parameter for \code{glmnet}.
Default is \code{0.1}.}

\item{seed}{Random seed for reproducibility. Default is \code{42}.}

\item{cv}{Logical. Whether to use \code{cv.glmnet} for
cross-validation. Default is \code{FALSE}.}

\item{nfolds}{Number of folds for cross-validation. Default is \code{5}.}

\item{lambda}{Optional lambda value for prediction. If \code{NULL},
uses \code{lambda.min} from CV or defaults to \code{0.01}.}

\item{return_probs}{Logical. If \code{TRUE},
returns full class probability matrix.
Default is \code{FALSE}.}

\item{return_confidence}{Logical. If \code{TRUE}, returns prediction
with confidence score and status.
Default is \code{TRUE}.}

\item{threshold}{Optional numeric threshold.
If set and \code{return_confidence = TRUE},
predictions below this confidence are labeled
as \code{"unknown"}.}

\item{return_model}{Logical. If \code{TRUE}, returns a list containing
both predictions and the trained model.
Default is \code{FALSE}.}

\item{...}{Additional arguments passed to
\code{\link{ComputeRankedMatrix}}.}
}
\value{
A data frame of predictions (by default),
or a list with elements \code{predictions} and
\code{model} if \code{return_model = TRUE}.
}
\description{
A unified function that trains a multinomial classifier on
reference expression data and predicts cell types for a new dataset
using top-k ranked genes. Automatically matches genes between datasets,
optionally performs cross-validation, and returns predictions
with confidence scores or probabilities.
}
\examples{
# ref_mat <- matrix(runif(1000), nrow = 100)
# test_mat <- matrix(runif(1000), nrow = 100)
# labels <- sample(c("A", "B"), ncol(ref_mat), TRUE)
# result <- RankMap(data = ref_mat, labels = labels,
#                   new_data = test_mat, return_model = TRUE)

}
