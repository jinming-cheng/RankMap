% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_model.R
\name{PredictRankModel}
\alias{PredictRankModel}
\title{Predict Cell Types, Probabilities, or Confidence Scores}
\usage{
PredictRankModel(
  model,
  new_data,
  lambda = NULL,
  return_probs = FALSE,
  return_confidence = FALSE,
  ...
)
}
\arguments{
\item{model}{A fitted model from \code{\link{TrainRankModel}}.}

\item{new_data}{A gene expression matrix with genes as rows and cells
(or spatial spots) as columns. Can be a Seurat object,
a dense numeric matrix, or a sparse \code{dgCMatrix}.}

\item{lambda}{Optional lambda value. If \code{NULL},
uses \code{lambda.min} (if available), else \code{0.01}.}

\item{return_probs}{Logical. If \code{TRUE}, returns the
full matrix of class probabilities.}

\item{return_confidence}{Logical. If \code{TRUE}, returns a data frame
with predicted labels and confidence scores.}

\item{...}{Additional arguments passed to
\code{\link{ComputeRankedMatrix}}.}
}
\value{
A character vector (default),
a matrix (if \code{return_probs = TRUE}),
or a data frame (if \code{return_confidence = TRUE}).
}
\description{
Predicts cell type labels or class probabilities using a trained
RankMap model. Supports both \code{glmnet} and \code{cv.glmnet}.
Optionally returns a data frame with predicted cell types
and confidence scores.
}
